# Hardhat raffle

## Dependency Introduction

```json
{
  "devDependencies": {
    "@chainlink/contracts": "^1.1.1", // Chainlink smart contracts
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.7", // Hardhat utils for testing
    "@nomicfoundation/hardhat-ethers": "^3.0.6", // Hardhat plugin for ethers, Add ethers to the Hardhat environment
    "@nomicfoundation/hardhat-network-helpers": "^1.0.11", // Hardhat utils for testing
    "@nomicfoundation/hardhat-verify": "^2.0.8", // Hardhat plugin for verifying contracts
    "@typechain/ethers-v6": "^0.5.1", //  TypeChain target for ethers-v6
    "@typechain/hardhat": "^9.1.0", // Zero-config TypeChain support for Hardhat
    "@types/chai": "^4.3.16", // TypeScript definitions for chai (@chainlink/contracts introduces chai)
    "@types/mocha": "^10.0.7", // TypeScript definitions for mocha (hatdhat introduced mocha)
    "ethers": "^6.13.1", // A library for Ethereum
    "hardhat": "^2.22.6", // Hardhat developer tool
    "hardhat-deploy": "^0.12.4", // Hardhat Plugin For Replicable Deployments And Tests
    "hardhat-deploy-ethers": "^0.4.2", // Support for hardhat-deploy, These helpers are added to the ethers object
    "ts-node": "^10.9.2", // TypeScript execution environment
    "tsconfig-paths": "^4.2.0", // Load node modules according to tsconfig paths
    "typechain": "^8.3.2", //  TypeScript bindings for Ethereum smartcontracts
    "typescript": "^5.5.3" // TypeScript is a language for application scale JavaScript development
  }
}
```
